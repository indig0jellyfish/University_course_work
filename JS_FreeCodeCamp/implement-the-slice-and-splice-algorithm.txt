** start of script.js **

const frankenSplice = (array1, array2, index) => {
  let newArray = array2.slice();
  newArray.splice(index, 0, ...array1); 
  return newArray;
}

** end of script.js **

