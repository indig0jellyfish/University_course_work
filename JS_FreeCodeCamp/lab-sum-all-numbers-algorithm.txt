** start of script.js **

const sumAll = (array) => {

  let result= 0
  let min = Math.min(array[0], array[1]);
  let max = Math.max(array[0], array[1]);

  for (let i = min; i <= max; i++) {
    result = result + i;
  }
  return result
}

** end of script.js **

