** start of script.js **

function smallestCommons(arr) {
  // Sort numbers in ascending order
  arr.sort(function(a, b) {
    return a - b;
  });

  var min = arr[0];
  var max = arr[1];

  // Greatest Common Divisor (Euclidean algorithm)
  function gcd(a, b) {
    if (b === 0) {
      return a;
    } else {
      return gcd(b, a % b);
    }
  }

  // Least Common Multiple
  function lcm(a, b) {
    return (a * b) / gcd(a, b);
  }

  var multiple = min;

  // Build LCM across the range
  for (var i = min + 1; i <= max; i++) {
    multiple = lcm(multiple, i);
  }

  return multiple;
}

** end of script.js **

